<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoMauiClient.Pages.ManageToDoPage"
             Title="Menu">
    <VerticalStackLayout>
        <Label Text="Login" FontSize="Large" HorizontalOptions="Center"/>
        <Entry Text="{Binding Path=ToDo.ToDoName}" Placeholder="Account" />
        <Entry Text="{Binding Path=ToDo.ToDoName}" Placeholder="Password" />
        <Button Text="Login"/>
    </VerticalStackLayout>
</ContentPage>